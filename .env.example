# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True

# VirusTotal API Configuration
# Get your API key from https://www.virustotal.com/gui/my-apikey
VIRUSTOTAL_API_KEY=your-virustotal-api-key-here

# Disable VirusTotal for testing (set to True to skip malware scanning)
# When True, all files will be accepted without VirusTotal API calls
DISABLE_VIRUSTOTAL=True

# Database (SQLite is default, no configuration needed)
# DATABASE_NAME=db.sqlite3

# File Upload Settings (configured in settings.py)
# MAX_FILE_SIZE=200MB
# LARGE_FILE_THRESHOLD=5MB
